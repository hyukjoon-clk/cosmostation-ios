// DO NOT EDIT.
// swift-format-ignore-file
//
// Generated by the Swift generator plugin for the protocol buffer compiler.
// Source: sui/rpc/v2/query_options.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/apple/swift-protobuf/

// Copyright (c) Mysten Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

import Foundation
import SwiftProtobuf

// If the compiler emits an error on this type, it is because this file
// was generated by a version of the `protoc` Swift plug-in that is
// incompatible with the version of SwiftProtobuf to which you are linking.
// Please ensure that you are building against the same version of the API
// that was used to generate this file.
fileprivate struct _GeneratedWithProtocGenSwiftVersion: SwiftProtobuf.ProtobufAPIVersionCheck {
  struct _2: SwiftProtobuf.ProtobufAPIVersion_2 {}
  typealias Version = _2
}

/// Ordering for the returned result set.
enum Sui_Rpc_V2_Ordering: SwiftProtobuf.Enum {
  typealias RawValue = Int

  /// Return results in increasing cursor order.
  case ascending // = 0

  /// Return results in decreasing cursor order.
  case descending // = 1
  case UNRECOGNIZED(Int)

  init() {
    self = .ascending
  }

  init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .ascending
    case 1: self = .descending
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  var rawValue: Int {
    switch self {
    case .ascending: return 0
    case .descending: return 1
    case .UNRECOGNIZED(let i): return i
    }
  }

}

#if swift(>=4.2)

extension Sui_Rpc_V2_Ordering: CaseIterable {
  // The compiler won't synthesize support with the UNRECOGNIZED case.
  static var allCases: [Sui_Rpc_V2_Ordering] = [
    .ascending,
    .descending,
  ]
}

#endif  // swift(>=4.2)

/// Reason the server stopped this query response.
enum Sui_Rpc_V2_QueryEndReason: SwiftProtobuf.Enum {
  typealias RawValue = Int

  /// The stop reason was not specified.
  case unknown // = 0

  /// The response reached the requested item limit. The final matching item's
  /// frame carries `QueryEnd` and the final watermark. Resume from that frame's
  /// `Watermark.cursor` to continue reading the same effective interval.
  case itemLimit // = 1

  /// The response reached the server's per-request bucket-fetch budget for
  /// filtered scans before reaching the effective interval bound. The terminal
  /// frame carries no item. Its watermark cursor is the authoritative scan
  /// frontier from which to resume.
  case scanLimit // = 2

  /// The scan reached a requested checkpoint range bound. The terminal frame
  /// carries no item.
  case checkpointBound // = 3

  /// The scan reached an exclusive cursor bound. The terminal frame carries no
  /// item. Its watermark cursor represents that resolved bound without claiming
  /// that its containing checkpoint was fully covered.
  case cursorBound // = 4

  /// The scan reached the currently indexed ledger tip. The terminal frame
  /// carries no item.
  case ledgerTip // = 5
  case UNRECOGNIZED(Int)

  init() {
    self = .unknown
  }

  init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unknown
    case 1: self = .itemLimit
    case 2: self = .scanLimit
    case 3: self = .checkpointBound
    case 4: self = .cursorBound
    case 5: self = .ledgerTip
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  var rawValue: Int {
    switch self {
    case .unknown: return 0
    case .itemLimit: return 1
    case .scanLimit: return 2
    case .checkpointBound: return 3
    case .cursorBound: return 4
    case .ledgerTip: return 5
    case .UNRECOGNIZED(let i): return i
    }
  }

}

#if swift(>=4.2)

extension Sui_Rpc_V2_QueryEndReason: CaseIterable {
  // The compiler won't synthesize support with the UNRECOGNIZED case.
  static var allCases: [Sui_Rpc_V2_QueryEndReason] = [
    .unknown,
    .itemLimit,
    .scanLimit,
    .checkpointBound,
    .cursorBound,
    .ledgerTip,
  ]
}

#endif  // swift(>=4.2)

/// Cursor-bounded query options.
///
/// `after` and `before` are canonical ledger-position bounds, not
/// ordering-relative cursors. `after` always excludes items at or below that
/// cursor, and `before` always excludes items at or above that cursor. Ordering
/// only controls the order of returned items within the resulting open interval.
///
/// When a request also specifies a checkpoint range, cursor bounds and
/// checkpoint bounds compose by intersection: results come only from ledger
/// positions inside both. Checkpoint bounds are likewise canonical and
/// ordering-independent.
///
/// For example, with `after = A`, `before = B`, `ordering = DESCENDING`, and
/// `limit = N`, the response contains up to N matching items in descending
/// order from the interval `(A, B)`. If the response ends with
/// `QUERY_END_REASON_ITEM_LIMIT`, resume by keeping `after = A` and setting
/// `before` to the last `Watermark.cursor` received. That cursor is the
/// lowest position reached in ledger order, so it becomes the next exclusive
/// upper bound.
struct Sui_Rpc_V2_QueryOptions {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// The maximum number of items to return. Each method applies its own default
  /// and maximum. QueryEnd does not count against this limit.
  var limit: UInt32 {
    get {return _limit ?? 0}
    set {_limit = newValue}
  }
  /// Returns true if `limit` has been explicitly set.
  var hasLimit: Bool {return self._limit != nil}
  /// Clears the value of `limit`. Subsequent reads from it will return its default value.
  mutating func clearLimit() {self._limit = nil}

  /// Opaque exclusive lower bound. Results must be strictly after this cursor in
  /// canonical ledger order.
  var after: Data {
    get {return _after ?? Data()}
    set {_after = newValue}
  }
  /// Returns true if `after` has been explicitly set.
  var hasAfter: Bool {return self._after != nil}
  /// Clears the value of `after`. Subsequent reads from it will return its default value.
  mutating func clearAfter() {self._after = nil}

  /// Opaque exclusive upper bound. Results must be strictly before this cursor
  /// in canonical ledger order.
  var before: Data {
    get {return _before ?? Data()}
    set {_before = newValue}
  }
  /// Returns true if `before` has been explicitly set.
  var hasBefore: Bool {return self._before != nil}
  /// Clears the value of `before`. Subsequent reads from it will return its default value.
  mutating func clearBefore() {self._before = nil}

  /// Ordering for returned results. Defaults to ASCENDING.
  ///
  /// Ordering only controls the order of results within the bounded interval;
  /// cursor bounds keep the same meaning for ascending and descending reads.
  var ordering: Sui_Rpc_V2_Ordering {
    get {return _ordering ?? .ascending}
    set {_ordering = newValue}
  }
  /// Returns true if `ordering` has been explicitly set.
  var hasOrdering: Bool {return self._ordering != nil}
  /// Clears the value of `ordering`. Subsequent reads from it will return its default value.
  mutating func clearOrdering() {self._ordering = nil}

  var unknownFields = SwiftProtobuf.UnknownStorage()

  init() {}

  fileprivate var _limit: UInt32? = nil
  fileprivate var _after: Data? = nil
  fileprivate var _before: Data? = nil
  fileprivate var _ordering: Sui_Rpc_V2_Ordering? = nil
}

/// Progress marker for a query scan. Carried on every response frame, whether or
/// not the frame delivers a matching item. Watermarks never regress in the
/// requested ordering, but consecutive frames may carry the same watermark when
/// additional work does not advance the safe resume frontier.
struct Sui_Rpc_V2_Watermark {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// Opaque cursor at this scan position. Set on every watermark. Use as
  /// `options.after` (ascending) or `options.before` (descending) on the next
  /// request to resume from here. The most recently received cursor is always
  /// the safe resume point.
  var cursor: Data {
    get {return _cursor ?? Data()}
    set {_cursor = newValue}
  }
  /// Returns true if `cursor` has been explicitly set.
  var hasCursor: Bool {return self._cursor != nil}
  /// Clears the value of `cursor`. Subsequent reads from it will return its default value.
  mutating func clearCursor() {self._cursor = nil}

  /// The inclusive boundary checkpoint that the scan has fully covered within
  /// the request's effective interval, in the request's ordering direction: an
  /// ascending scan has emitted every matching item in the interval at
  /// checkpoints `<= checkpoint` (strictly greater ones may still hold
  /// matches); a descending scan has emitted every matching item in the
  /// interval at checkpoints `>= checkpoint`. This boundary never regresses in
  /// the scan direction, but it may repeat while the cursor advances.
  ///
  /// Unset until the scan's first checkpoint is fully covered. For example, a
  /// scan resumed from a cursor that lands mid-checkpoint leaves this unset
  /// until the next checkpoint boundary in the scan direction is fully covered.
  /// A watermark still has a valid resume cursor while this field is unset.
  var checkpoint: UInt64 {
    get {return _checkpoint ?? 0}
    set {_checkpoint = newValue}
  }
  /// Returns true if `checkpoint` has been explicitly set.
  var hasCheckpoint: Bool {return self._checkpoint != nil}
  /// Clears the value of `checkpoint`. Subsequent reads from it will return its default value.
  mutating func clearCheckpoint() {self._checkpoint = nil}

  var unknownFields = SwiftProtobuf.UnknownStorage()

  init() {}

  fileprivate var _cursor: Data? = nil
  fileprivate var _checkpoint: UInt64? = nil
}

/// Marker for the final frame of a successful query stream. Every successful
/// stream sets `QueryEnd` on exactly one frame, after which no further frames
/// are sent. That frame always carries the final watermark. For `ItemLimit`, it
/// also carries the final matching item; for every other reason it carries no
/// item. A ScanLimit terminal watermark may repeat the previous frame's cursor
/// when its authoritative scan frontier was already emitted; this does not
/// repeat an item.
///
/// A stream that fails or is cancelled terminates with a gRPC status and does
/// not send `QueryEnd`.
struct Sui_Rpc_V2_QueryEnd {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// Reason this response stopped.
  var reason: Sui_Rpc_V2_QueryEndReason {
    get {return _reason ?? .unknown}
    set {_reason = newValue}
  }
  /// Returns true if `reason` has been explicitly set.
  var hasReason: Bool {return self._reason != nil}
  /// Clears the value of `reason`. Subsequent reads from it will return its default value.
  mutating func clearReason() {self._reason = nil}

  var unknownFields = SwiftProtobuf.UnknownStorage()

  init() {}

  fileprivate var _reason: Sui_Rpc_V2_QueryEndReason? = nil
}

#if swift(>=5.5) && canImport(_Concurrency)
extension Sui_Rpc_V2_Ordering: @unchecked Sendable {}
extension Sui_Rpc_V2_QueryEndReason: @unchecked Sendable {}
extension Sui_Rpc_V2_QueryOptions: @unchecked Sendable {}
extension Sui_Rpc_V2_Watermark: @unchecked Sendable {}
extension Sui_Rpc_V2_QueryEnd: @unchecked Sendable {}
#endif  // swift(>=5.5) && canImport(_Concurrency)

// MARK: - Code below here is support for the SwiftProtobuf runtime.

fileprivate let _protobuf_package = "sui.rpc.v2"

extension Sui_Rpc_V2_Ordering: SwiftProtobuf._ProtoNameProviding {
  static let _protobuf_nameMap: SwiftProtobuf._NameMap = [
    0: .same(proto: "ORDERING_ASCENDING"),
    1: .same(proto: "ORDERING_DESCENDING"),
  ]
}

extension Sui_Rpc_V2_QueryEndReason: SwiftProtobuf._ProtoNameProviding {
  static let _protobuf_nameMap: SwiftProtobuf._NameMap = [
    0: .same(proto: "QUERY_END_REASON_UNKNOWN"),
    1: .same(proto: "QUERY_END_REASON_ITEM_LIMIT"),
    2: .same(proto: "QUERY_END_REASON_SCAN_LIMIT"),
    3: .same(proto: "QUERY_END_REASON_CHECKPOINT_BOUND"),
    4: .same(proto: "QUERY_END_REASON_CURSOR_BOUND"),
    5: .same(proto: "QUERY_END_REASON_LEDGER_TIP"),
  ]
}

extension Sui_Rpc_V2_QueryOptions: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  static let protoMessageName: String = _protobuf_package + ".QueryOptions"
  static let _protobuf_nameMap: SwiftProtobuf._NameMap = [
    1: .same(proto: "limit"),
    2: .same(proto: "after"),
    3: .same(proto: "before"),
    4: .same(proto: "ordering"),
  ]

  mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    while let fieldNumber = try decoder.nextFieldNumber() {
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every case branch when no optimizations are
      // enabled. https://github.com/apple/swift-protobuf/issues/1034
      switch fieldNumber {
      case 1: try { try decoder.decodeSingularUInt32Field(value: &self._limit) }()
      case 2: try { try decoder.decodeSingularBytesField(value: &self._after) }()
      case 3: try { try decoder.decodeSingularBytesField(value: &self._before) }()
      case 4: try { try decoder.decodeSingularEnumField(value: &self._ordering) }()
      default: break
      }
    }
  }

  func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    // The use of inline closures is to circumvent an issue where the compiler
    // allocates stack space for every if/case branch local when no optimizations
    // are enabled. https://github.com/apple/swift-protobuf/issues/1034 and
    // https://github.com/apple/swift-protobuf/issues/1182
    try { if let v = self._limit {
      try visitor.visitSingularUInt32Field(value: v, fieldNumber: 1)
    } }()
    try { if let v = self._after {
      try visitor.visitSingularBytesField(value: v, fieldNumber: 2)
    } }()
    try { if let v = self._before {
      try visitor.visitSingularBytesField(value: v, fieldNumber: 3)
    } }()
    try { if let v = self._ordering {
      try visitor.visitSingularEnumField(value: v, fieldNumber: 4)
    } }()
    try unknownFields.traverse(visitor: &visitor)
  }

  static func ==(lhs: Sui_Rpc_V2_QueryOptions, rhs: Sui_Rpc_V2_QueryOptions) -> Bool {
    if lhs._limit != rhs._limit {return false}
    if lhs._after != rhs._after {return false}
    if lhs._before != rhs._before {return false}
    if lhs._ordering != rhs._ordering {return false}
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}

extension Sui_Rpc_V2_Watermark: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  static let protoMessageName: String = _protobuf_package + ".Watermark"
  static let _protobuf_nameMap: SwiftProtobuf._NameMap = [
    1: .same(proto: "cursor"),
    2: .same(proto: "checkpoint"),
  ]

  mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    while let fieldNumber = try decoder.nextFieldNumber() {
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every case branch when no optimizations are
      // enabled. https://github.com/apple/swift-protobuf/issues/1034
      switch fieldNumber {
      case 1: try { try decoder.decodeSingularBytesField(value: &self._cursor) }()
      case 2: try { try decoder.decodeSingularUInt64Field(value: &self._checkpoint) }()
      default: break
      }
    }
  }

  func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    // The use of inline closures is to circumvent an issue where the compiler
    // allocates stack space for every if/case branch local when no optimizations
    // are enabled. https://github.com/apple/swift-protobuf/issues/1034 and
    // https://github.com/apple/swift-protobuf/issues/1182
    try { if let v = self._cursor {
      try visitor.visitSingularBytesField(value: v, fieldNumber: 1)
    } }()
    try { if let v = self._checkpoint {
      try visitor.visitSingularUInt64Field(value: v, fieldNumber: 2)
    } }()
    try unknownFields.traverse(visitor: &visitor)
  }

  static func ==(lhs: Sui_Rpc_V2_Watermark, rhs: Sui_Rpc_V2_Watermark) -> Bool {
    if lhs._cursor != rhs._cursor {return false}
    if lhs._checkpoint != rhs._checkpoint {return false}
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}

extension Sui_Rpc_V2_QueryEnd: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  static let protoMessageName: String = _protobuf_package + ".QueryEnd"
  static let _protobuf_nameMap: SwiftProtobuf._NameMap = [
    1: .same(proto: "reason"),
  ]

  mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    while let fieldNumber = try decoder.nextFieldNumber() {
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every case branch when no optimizations are
      // enabled. https://github.com/apple/swift-protobuf/issues/1034
      switch fieldNumber {
      case 1: try { try decoder.decodeSingularEnumField(value: &self._reason) }()
      default: break
      }
    }
  }

  func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    // The use of inline closures is to circumvent an issue where the compiler
    // allocates stack space for every if/case branch local when no optimizations
    // are enabled. https://github.com/apple/swift-protobuf/issues/1034 and
    // https://github.com/apple/swift-protobuf/issues/1182
    try { if let v = self._reason {
      try visitor.visitSingularEnumField(value: v, fieldNumber: 1)
    } }()
    try unknownFields.traverse(visitor: &visitor)
  }

  static func ==(lhs: Sui_Rpc_V2_QueryEnd, rhs: Sui_Rpc_V2_QueryEnd) -> Bool {
    if lhs._reason != rhs._reason {return false}
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}
