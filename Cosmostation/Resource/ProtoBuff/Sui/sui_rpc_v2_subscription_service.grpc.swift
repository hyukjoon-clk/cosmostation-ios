//
// DO NOT EDIT.
//
// Generated by the protocol buffer compiler.
// Source: sui/rpc/v2/subscription_service.proto
//

//
// Copyright 2018, gRPC Authors All rights reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
import GRPC
import NIO
import NIOConcurrencyHelpers
import SwiftProtobuf


/// SubscriptionService provides filtered, real-time streams of checkpoints,
/// transactions, and events.
///
/// Each Subscribe API pairs with the LedgerService List API of the same name:
/// requests take the same filter message, and responses carry the same item
/// and watermark shapes with identical cursor semantics.
///
/// Subscriptions do not support resumption. A new subscription always begins
/// at the current tip of the chain as seen by the server (the latest executed
/// checkpoint). To recover data missed between subscriptions, replay the gap
/// with the paired List API: pass the last received `Watermark.cursor` as
/// `options.after` on the List request (for checkpoints, pass the last
/// received `cursor + 1` as `start_checkpoint`). The List scan reads from the
/// indexed tip, which may trail the subscription's start position; repeat the
/// List call as the index advances until the replay reaches the position
/// established by the subscription's first frame.
///
/// A subscription behaves like an unbounded ascending scan: every frame
/// carries the subscriber's resume point, and progress advances as
/// checkpoints are fully covered. Two delivery guarantees keep sparse
/// filters live: the first frame on a filtered subscription is a
/// progress-only frame establishing the stream's start position, and
/// progress continues to advance with bounded staleness even when no item
/// matches.
///
/// Subscription streams have no successful end: they run until cancelled by
/// the client or terminated by the server with a gRPC status.
///
/// Usage: instantiate `Sui_Rpc_V2_SubscriptionServiceClient`, then call methods of this protocol to make API calls.
internal protocol Sui_Rpc_V2_SubscriptionServiceClientProtocol: GRPCClient {
  var serviceName: String { get }
  var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? { get }

  func subscribeCheckpoints(
    _ request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    callOptions: CallOptions?,
    handler: @escaping (Sui_Rpc_V2_SubscribeCheckpointsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse>

  func subscribeTransactions(
    _ request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    callOptions: CallOptions?,
    handler: @escaping (Sui_Rpc_V2_SubscribeTransactionsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse>

  func subscribeEvents(
    _ request: Sui_Rpc_V2_SubscribeEventsRequest,
    callOptions: CallOptions?,
    handler: @escaping (Sui_Rpc_V2_SubscribeEventsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse>
}

extension Sui_Rpc_V2_SubscriptionServiceClientProtocol {
  internal var serviceName: String {
    return "sui.rpc.v2.SubscriptionService"
  }

  /// Subscribe to the stream of checkpoints.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields checkpoints matching the filter as they are executed.
  /// A checkpoint matches if any transaction it contains satisfies the
  /// filter.
  ///
  /// - Parameters:
  ///   - request: Request to send to SubscribeCheckpoints.
  ///   - callOptions: Call options.
  ///   - handler: A closure called when each response is received from the server.
  /// - Returns: A `ServerStreamingCall` with futures for the metadata and status.
  internal func subscribeCheckpoints(
    _ request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    callOptions: CallOptions? = nil,
    handler: @escaping (Sui_Rpc_V2_SubscribeCheckpointsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse> {
    return self.makeServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeCheckpoints.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeCheckpointsInterceptors() ?? [],
      handler: handler
    )
  }

  /// Subscribe to the stream of transactions.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields transactions matching the filter as they are executed.
  ///
  /// - Parameters:
  ///   - request: Request to send to SubscribeTransactions.
  ///   - callOptions: Call options.
  ///   - handler: A closure called when each response is received from the server.
  /// - Returns: A `ServerStreamingCall` with futures for the metadata and status.
  internal func subscribeTransactions(
    _ request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    callOptions: CallOptions? = nil,
    handler: @escaping (Sui_Rpc_V2_SubscribeTransactionsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse> {
    return self.makeServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeTransactions.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeTransactionsInterceptors() ?? [],
      handler: handler
    )
  }

  /// Subscribe to the stream of events.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields events matching the filter as they are emitted.
  ///
  /// - Parameters:
  ///   - request: Request to send to SubscribeEvents.
  ///   - callOptions: Call options.
  ///   - handler: A closure called when each response is received from the server.
  /// - Returns: A `ServerStreamingCall` with futures for the metadata and status.
  internal func subscribeEvents(
    _ request: Sui_Rpc_V2_SubscribeEventsRequest,
    callOptions: CallOptions? = nil,
    handler: @escaping (Sui_Rpc_V2_SubscribeEventsResponse) -> Void
  ) -> ServerStreamingCall<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse> {
    return self.makeServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeEvents.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeEventsInterceptors() ?? [],
      handler: handler
    )
  }
}

@available(*, deprecated)
extension Sui_Rpc_V2_SubscriptionServiceClient: @unchecked Sendable {}

@available(*, deprecated, renamed: "Sui_Rpc_V2_SubscriptionServiceNIOClient")
internal final class Sui_Rpc_V2_SubscriptionServiceClient: Sui_Rpc_V2_SubscriptionServiceClientProtocol {
  private let lock = Lock()
  private var _defaultCallOptions: CallOptions
  private var _interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol?
  internal let channel: GRPCChannel
  internal var defaultCallOptions: CallOptions {
    get { self.lock.withLock { return self._defaultCallOptions } }
    set { self.lock.withLockVoid { self._defaultCallOptions = newValue } }
  }
  internal var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? {
    get { self.lock.withLock { return self._interceptors } }
    set { self.lock.withLockVoid { self._interceptors = newValue } }
  }

  /// Creates a client for the sui.rpc.v2.SubscriptionService service.
  ///
  /// - Parameters:
  ///   - channel: `GRPCChannel` to the service host.
  ///   - defaultCallOptions: Options to use for each service call if the user doesn't provide them.
  ///   - interceptors: A factory providing interceptors for each RPC.
  internal init(
    channel: GRPCChannel,
    defaultCallOptions: CallOptions = CallOptions(),
    interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? = nil
  ) {
    self.channel = channel
    self._defaultCallOptions = defaultCallOptions
    self._interceptors = interceptors
  }
}

internal struct Sui_Rpc_V2_SubscriptionServiceNIOClient: Sui_Rpc_V2_SubscriptionServiceClientProtocol {
  internal var channel: GRPCChannel
  internal var defaultCallOptions: CallOptions
  internal var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol?

  /// Creates a client for the sui.rpc.v2.SubscriptionService service.
  ///
  /// - Parameters:
  ///   - channel: `GRPCChannel` to the service host.
  ///   - defaultCallOptions: Options to use for each service call if the user doesn't provide them.
  ///   - interceptors: A factory providing interceptors for each RPC.
  internal init(
    channel: GRPCChannel,
    defaultCallOptions: CallOptions = CallOptions(),
    interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? = nil
  ) {
    self.channel = channel
    self.defaultCallOptions = defaultCallOptions
    self.interceptors = interceptors
  }
}

/// SubscriptionService provides filtered, real-time streams of checkpoints,
/// transactions, and events.
///
/// Each Subscribe API pairs with the LedgerService List API of the same name:
/// requests take the same filter message, and responses carry the same item
/// and watermark shapes with identical cursor semantics.
///
/// Subscriptions do not support resumption. A new subscription always begins
/// at the current tip of the chain as seen by the server (the latest executed
/// checkpoint). To recover data missed between subscriptions, replay the gap
/// with the paired List API: pass the last received `Watermark.cursor` as
/// `options.after` on the List request (for checkpoints, pass the last
/// received `cursor + 1` as `start_checkpoint`). The List scan reads from the
/// indexed tip, which may trail the subscription's start position; repeat the
/// List call as the index advances until the replay reaches the position
/// established by the subscription's first frame.
///
/// A subscription behaves like an unbounded ascending scan: every frame
/// carries the subscriber's resume point, and progress advances as
/// checkpoints are fully covered. Two delivery guarantees keep sparse
/// filters live: the first frame on a filtered subscription is a
/// progress-only frame establishing the stream's start position, and
/// progress continues to advance with bounded staleness even when no item
/// matches.
///
/// Subscription streams have no successful end: they run until cancelled by
/// the client or terminated by the server with a gRPC status.
@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
internal protocol Sui_Rpc_V2_SubscriptionServiceAsyncClientProtocol: GRPCClient {
  static var serviceDescriptor: GRPCServiceDescriptor { get }
  var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? { get }

  func makeSubscribeCheckpointsCall(
    _ request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    callOptions: CallOptions?
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse>

  func makeSubscribeTransactionsCall(
    _ request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    callOptions: CallOptions?
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse>

  func makeSubscribeEventsCall(
    _ request: Sui_Rpc_V2_SubscribeEventsRequest,
    callOptions: CallOptions?
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse>
}

@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
extension Sui_Rpc_V2_SubscriptionServiceAsyncClientProtocol {
  internal static var serviceDescriptor: GRPCServiceDescriptor {
    return Sui_Rpc_V2_SubscriptionServiceClientMetadata.serviceDescriptor
  }

  internal var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? {
    return nil
  }

  internal func makeSubscribeCheckpointsCall(
    _ request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse> {
    return self.makeAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeCheckpoints.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeCheckpointsInterceptors() ?? []
    )
  }

  internal func makeSubscribeTransactionsCall(
    _ request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse> {
    return self.makeAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeTransactions.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeTransactionsInterceptors() ?? []
    )
  }

  internal func makeSubscribeEventsCall(
    _ request: Sui_Rpc_V2_SubscribeEventsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncServerStreamingCall<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse> {
    return self.makeAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeEvents.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeEventsInterceptors() ?? []
    )
  }
}

@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
extension Sui_Rpc_V2_SubscriptionServiceAsyncClientProtocol {
  internal func subscribeCheckpoints(
    _ request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncResponseStream<Sui_Rpc_V2_SubscribeCheckpointsResponse> {
    return self.performAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeCheckpoints.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeCheckpointsInterceptors() ?? []
    )
  }

  internal func subscribeTransactions(
    _ request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncResponseStream<Sui_Rpc_V2_SubscribeTransactionsResponse> {
    return self.performAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeTransactions.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeTransactionsInterceptors() ?? []
    )
  }

  internal func subscribeEvents(
    _ request: Sui_Rpc_V2_SubscribeEventsRequest,
    callOptions: CallOptions? = nil
  ) -> GRPCAsyncResponseStream<Sui_Rpc_V2_SubscribeEventsResponse> {
    return self.performAsyncServerStreamingCall(
      path: Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeEvents.path,
      request: request,
      callOptions: callOptions ?? self.defaultCallOptions,
      interceptors: self.interceptors?.makeSubscribeEventsInterceptors() ?? []
    )
  }
}

@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
internal struct Sui_Rpc_V2_SubscriptionServiceAsyncClient: Sui_Rpc_V2_SubscriptionServiceAsyncClientProtocol {
  internal var channel: GRPCChannel
  internal var defaultCallOptions: CallOptions
  internal var interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol?

  internal init(
    channel: GRPCChannel,
    defaultCallOptions: CallOptions = CallOptions(),
    interceptors: Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol? = nil
  ) {
    self.channel = channel
    self.defaultCallOptions = defaultCallOptions
    self.interceptors = interceptors
  }
}

internal protocol Sui_Rpc_V2_SubscriptionServiceClientInterceptorFactoryProtocol: Sendable {

  /// - Returns: Interceptors to use when invoking 'subscribeCheckpoints'.
  func makeSubscribeCheckpointsInterceptors() -> [ClientInterceptor<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse>]

  /// - Returns: Interceptors to use when invoking 'subscribeTransactions'.
  func makeSubscribeTransactionsInterceptors() -> [ClientInterceptor<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse>]

  /// - Returns: Interceptors to use when invoking 'subscribeEvents'.
  func makeSubscribeEventsInterceptors() -> [ClientInterceptor<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse>]
}

internal enum Sui_Rpc_V2_SubscriptionServiceClientMetadata {
  internal static let serviceDescriptor = GRPCServiceDescriptor(
    name: "SubscriptionService",
    fullName: "sui.rpc.v2.SubscriptionService",
    methods: [
      Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeCheckpoints,
      Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeTransactions,
      Sui_Rpc_V2_SubscriptionServiceClientMetadata.Methods.subscribeEvents,
    ]
  )

  internal enum Methods {
    internal static let subscribeCheckpoints = GRPCMethodDescriptor(
      name: "SubscribeCheckpoints",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeCheckpoints",
      type: GRPCCallType.serverStreaming
    )

    internal static let subscribeTransactions = GRPCMethodDescriptor(
      name: "SubscribeTransactions",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeTransactions",
      type: GRPCCallType.serverStreaming
    )

    internal static let subscribeEvents = GRPCMethodDescriptor(
      name: "SubscribeEvents",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeEvents",
      type: GRPCCallType.serverStreaming
    )
  }
}

/// SubscriptionService provides filtered, real-time streams of checkpoints,
/// transactions, and events.
///
/// Each Subscribe API pairs with the LedgerService List API of the same name:
/// requests take the same filter message, and responses carry the same item
/// and watermark shapes with identical cursor semantics.
///
/// Subscriptions do not support resumption. A new subscription always begins
/// at the current tip of the chain as seen by the server (the latest executed
/// checkpoint). To recover data missed between subscriptions, replay the gap
/// with the paired List API: pass the last received `Watermark.cursor` as
/// `options.after` on the List request (for checkpoints, pass the last
/// received `cursor + 1` as `start_checkpoint`). The List scan reads from the
/// indexed tip, which may trail the subscription's start position; repeat the
/// List call as the index advances until the replay reaches the position
/// established by the subscription's first frame.
///
/// A subscription behaves like an unbounded ascending scan: every frame
/// carries the subscriber's resume point, and progress advances as
/// checkpoints are fully covered. Two delivery guarantees keep sparse
/// filters live: the first frame on a filtered subscription is a
/// progress-only frame establishing the stream's start position, and
/// progress continues to advance with bounded staleness even when no item
/// matches.
///
/// Subscription streams have no successful end: they run until cancelled by
/// the client or terminated by the server with a gRPC status.
///
/// To build a server, implement a class that conforms to this protocol.
internal protocol Sui_Rpc_V2_SubscriptionServiceProvider: CallHandlerProvider {
  var interceptors: Sui_Rpc_V2_SubscriptionServiceServerInterceptorFactoryProtocol? { get }

  /// Subscribe to the stream of checkpoints.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields checkpoints matching the filter as they are executed.
  /// A checkpoint matches if any transaction it contains satisfies the
  /// filter.
  func subscribeCheckpoints(request: Sui_Rpc_V2_SubscribeCheckpointsRequest, context: StreamingResponseCallContext<Sui_Rpc_V2_SubscribeCheckpointsResponse>) -> EventLoopFuture<GRPCStatus>

  /// Subscribe to the stream of transactions.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields transactions matching the filter as they are executed.
  func subscribeTransactions(request: Sui_Rpc_V2_SubscribeTransactionsRequest, context: StreamingResponseCallContext<Sui_Rpc_V2_SubscribeTransactionsResponse>) -> EventLoopFuture<GRPCStatus>

  /// Subscribe to the stream of events.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields events matching the filter as they are emitted.
  func subscribeEvents(request: Sui_Rpc_V2_SubscribeEventsRequest, context: StreamingResponseCallContext<Sui_Rpc_V2_SubscribeEventsResponse>) -> EventLoopFuture<GRPCStatus>
}

extension Sui_Rpc_V2_SubscriptionServiceProvider {
  internal var serviceName: Substring {
    return Sui_Rpc_V2_SubscriptionServiceServerMetadata.serviceDescriptor.fullName[...]
  }

  /// Determines, calls and returns the appropriate request handler, depending on the request's method.
  /// Returns nil for methods not handled by this service.
  internal func handle(
    method name: Substring,
    context: CallHandlerContext
  ) -> GRPCServerHandlerProtocol? {
    switch name {
    case "SubscribeCheckpoints":
      return ServerStreamingServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeCheckpointsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeCheckpointsResponse>(),
        interceptors: self.interceptors?.makeSubscribeCheckpointsInterceptors() ?? [],
        userFunction: self.subscribeCheckpoints(request:context:)
      )

    case "SubscribeTransactions":
      return ServerStreamingServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeTransactionsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeTransactionsResponse>(),
        interceptors: self.interceptors?.makeSubscribeTransactionsInterceptors() ?? [],
        userFunction: self.subscribeTransactions(request:context:)
      )

    case "SubscribeEvents":
      return ServerStreamingServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeEventsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeEventsResponse>(),
        interceptors: self.interceptors?.makeSubscribeEventsInterceptors() ?? [],
        userFunction: self.subscribeEvents(request:context:)
      )

    default:
      return nil
    }
  }
}

/// SubscriptionService provides filtered, real-time streams of checkpoints,
/// transactions, and events.
///
/// Each Subscribe API pairs with the LedgerService List API of the same name:
/// requests take the same filter message, and responses carry the same item
/// and watermark shapes with identical cursor semantics.
///
/// Subscriptions do not support resumption. A new subscription always begins
/// at the current tip of the chain as seen by the server (the latest executed
/// checkpoint). To recover data missed between subscriptions, replay the gap
/// with the paired List API: pass the last received `Watermark.cursor` as
/// `options.after` on the List request (for checkpoints, pass the last
/// received `cursor + 1` as `start_checkpoint`). The List scan reads from the
/// indexed tip, which may trail the subscription's start position; repeat the
/// List call as the index advances until the replay reaches the position
/// established by the subscription's first frame.
///
/// A subscription behaves like an unbounded ascending scan: every frame
/// carries the subscriber's resume point, and progress advances as
/// checkpoints are fully covered. Two delivery guarantees keep sparse
/// filters live: the first frame on a filtered subscription is a
/// progress-only frame establishing the stream's start position, and
/// progress continues to advance with bounded staleness even when no item
/// matches.
///
/// Subscription streams have no successful end: they run until cancelled by
/// the client or terminated by the server with a gRPC status.
///
/// To implement a server, implement an object which conforms to this protocol.
@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
internal protocol Sui_Rpc_V2_SubscriptionServiceAsyncProvider: CallHandlerProvider {
  static var serviceDescriptor: GRPCServiceDescriptor { get }
  var interceptors: Sui_Rpc_V2_SubscriptionServiceServerInterceptorFactoryProtocol? { get }

  /// Subscribe to the stream of checkpoints.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields checkpoints matching the filter as they are executed.
  /// A checkpoint matches if any transaction it contains satisfies the
  /// filter.
  @Sendable func subscribeCheckpoints(
    request: Sui_Rpc_V2_SubscribeCheckpointsRequest,
    responseStream: GRPCAsyncResponseStreamWriter<Sui_Rpc_V2_SubscribeCheckpointsResponse>,
    context: GRPCAsyncServerCallContext
  ) async throws

  /// Subscribe to the stream of transactions.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields transactions matching the filter as they are executed.
  @Sendable func subscribeTransactions(
    request: Sui_Rpc_V2_SubscribeTransactionsRequest,
    responseStream: GRPCAsyncResponseStreamWriter<Sui_Rpc_V2_SubscribeTransactionsResponse>,
    context: GRPCAsyncServerCallContext
  ) async throws

  /// Subscribe to the stream of events.
  ///
  /// The stream begins at the latest executed checkpoint as seen by the
  /// server and yields events matching the filter as they are emitted.
  @Sendable func subscribeEvents(
    request: Sui_Rpc_V2_SubscribeEventsRequest,
    responseStream: GRPCAsyncResponseStreamWriter<Sui_Rpc_V2_SubscribeEventsResponse>,
    context: GRPCAsyncServerCallContext
  ) async throws
}

@available(macOS 10.15, iOS 13, tvOS 13, watchOS 6, *)
extension Sui_Rpc_V2_SubscriptionServiceAsyncProvider {
  internal static var serviceDescriptor: GRPCServiceDescriptor {
    return Sui_Rpc_V2_SubscriptionServiceServerMetadata.serviceDescriptor
  }

  internal var serviceName: Substring {
    return Sui_Rpc_V2_SubscriptionServiceServerMetadata.serviceDescriptor.fullName[...]
  }

  internal var interceptors: Sui_Rpc_V2_SubscriptionServiceServerInterceptorFactoryProtocol? {
    return nil
  }

  internal func handle(
    method name: Substring,
    context: CallHandlerContext
  ) -> GRPCServerHandlerProtocol? {
    switch name {
    case "SubscribeCheckpoints":
      return GRPCAsyncServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeCheckpointsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeCheckpointsResponse>(),
        interceptors: self.interceptors?.makeSubscribeCheckpointsInterceptors() ?? [],
        wrapping: self.subscribeCheckpoints(request:responseStream:context:)
      )

    case "SubscribeTransactions":
      return GRPCAsyncServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeTransactionsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeTransactionsResponse>(),
        interceptors: self.interceptors?.makeSubscribeTransactionsInterceptors() ?? [],
        wrapping: self.subscribeTransactions(request:responseStream:context:)
      )

    case "SubscribeEvents":
      return GRPCAsyncServerHandler(
        context: context,
        requestDeserializer: ProtobufDeserializer<Sui_Rpc_V2_SubscribeEventsRequest>(),
        responseSerializer: ProtobufSerializer<Sui_Rpc_V2_SubscribeEventsResponse>(),
        interceptors: self.interceptors?.makeSubscribeEventsInterceptors() ?? [],
        wrapping: self.subscribeEvents(request:responseStream:context:)
      )

    default:
      return nil
    }
  }
}

internal protocol Sui_Rpc_V2_SubscriptionServiceServerInterceptorFactoryProtocol {

  /// - Returns: Interceptors to use when handling 'subscribeCheckpoints'.
  ///   Defaults to calling `self.makeInterceptors()`.
  func makeSubscribeCheckpointsInterceptors() -> [ServerInterceptor<Sui_Rpc_V2_SubscribeCheckpointsRequest, Sui_Rpc_V2_SubscribeCheckpointsResponse>]

  /// - Returns: Interceptors to use when handling 'subscribeTransactions'.
  ///   Defaults to calling `self.makeInterceptors()`.
  func makeSubscribeTransactionsInterceptors() -> [ServerInterceptor<Sui_Rpc_V2_SubscribeTransactionsRequest, Sui_Rpc_V2_SubscribeTransactionsResponse>]

  /// - Returns: Interceptors to use when handling 'subscribeEvents'.
  ///   Defaults to calling `self.makeInterceptors()`.
  func makeSubscribeEventsInterceptors() -> [ServerInterceptor<Sui_Rpc_V2_SubscribeEventsRequest, Sui_Rpc_V2_SubscribeEventsResponse>]
}

internal enum Sui_Rpc_V2_SubscriptionServiceServerMetadata {
  internal static let serviceDescriptor = GRPCServiceDescriptor(
    name: "SubscriptionService",
    fullName: "sui.rpc.v2.SubscriptionService",
    methods: [
      Sui_Rpc_V2_SubscriptionServiceServerMetadata.Methods.subscribeCheckpoints,
      Sui_Rpc_V2_SubscriptionServiceServerMetadata.Methods.subscribeTransactions,
      Sui_Rpc_V2_SubscriptionServiceServerMetadata.Methods.subscribeEvents,
    ]
  )

  internal enum Methods {
    internal static let subscribeCheckpoints = GRPCMethodDescriptor(
      name: "SubscribeCheckpoints",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeCheckpoints",
      type: GRPCCallType.serverStreaming
    )

    internal static let subscribeTransactions = GRPCMethodDescriptor(
      name: "SubscribeTransactions",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeTransactions",
      type: GRPCCallType.serverStreaming
    )

    internal static let subscribeEvents = GRPCMethodDescriptor(
      name: "SubscribeEvents",
      path: "/sui.rpc.v2.SubscriptionService/SubscribeEvents",
      type: GRPCCallType.serverStreaming
    )
  }
}
